<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.ai.mapper.MessageMapper">

    <resultMap id="BaseResultMap" type="com.example.ai.entity.Message">
        <id column="id" property="id"/>
        <result column="conversation_id" property="conversationId"/>
        <result column="role" property="role"/>
        <result column="content" property="content"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO message (conversation_id, role, content, created_at)
        VALUES (#{conversationId}, #{role}, #{content}, NOW())
    </insert>

    <select id="listByConversationId" resultMap="BaseResultMap">
        SELECT id, conversation_id, role, content, created_at
        FROM message
        WHERE conversation_id = #{conversationId}
        ORDER BY id ASC
    </select>

    <select id="listRecentMessages" resultMap="BaseResultMap">
        SELECT id, conversation_id, role, content, created_at
        FROM message
        WHERE conversation_id = #{conversationId}
        ORDER BY id DESC
        LIMIT #{limit}
    </select>

</mapper>
